[project]
name = "kiss-router"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["PyYAML>=6.0"]

[project.scripts]
tiny-router = "kiss_router.server:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
